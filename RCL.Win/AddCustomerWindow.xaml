<Window x:Class="RCL.Win.AddCustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Customer" Height="360" Width="560"
        Style="{DynamicResource DialogWindowStyle}"
        ResizeMode="NoResize">
  <Border Style="{DynamicResource CardBorder}">
    <Grid Margin="8">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <TextBlock Text="Add Customer" FontWeight="Bold" FontSize="{DynamicResource AppFontSize}" Margin="0,0,0,6"/>

      <StackPanel Grid.Row="1" Orientation="Vertical">
        <Grid Margin="0,6">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <TextBlock Text="Full name" VerticalAlignment="Center" Grid.Column="0"/>
          <TextBox x:Name="TxtFullName" Grid.Column="1" MinWidth="280"/>
        </Grid>

        <Grid Margin="0,6">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <TextBlock Text="Phone number" VerticalAlignment="Center" Grid.Column="0"/>
          <TextBox x:Name="TxtPhone" Grid.Column="1" MinWidth="280" />
        </Grid>

        <Grid Margin="0,6">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <TextBlock Text="Email (optional)" VerticalAlignment="Center" Grid.Column="0"/>
          <TextBox x:Name="TxtEmail" Grid.Column="1" MinWidth="280" />
        </Grid>

        <TextBlock x:Name="ValidationText" Foreground="Red" Visibility="Collapsed" Margin="0,6,0,0"/>
      </StackPanel>

      <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
        <Button x:Name="BtnCancel" Content="Cancel" Style="{DynamicResource SecondaryButton}" Width="110" Click="BtnCancel_Click"/>
        <Button x:Name="BtnSave" Content="Save" Style="{DynamicResource PrimaryButton}" Width="110" Margin="8,0,0,0" Click="BtnSave_Click"/>
      </StackPanel>
    </Grid>
  </Border>
</Window>

