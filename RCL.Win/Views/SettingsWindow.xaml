<Window x:Class="RCL.Win.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Width="420" Height="340" WindowStartupLocation="CenterOwner">
    <StackPanel Margin="12">
        <TextBlock Text="Theme" FontWeight="Bold"/>
        <StackPanel Orientation="Horizontal" Margin="0,6,0,12">
            <Button x:Name="LightBtn" Content="Light"/>
            <Button x:Name="DarkBtn" Content="Dark" Margin="6,0,0,0"/>
        </StackPanel>

        <TextBlock Text="Text size" FontWeight="Bold"/>
        <StackPanel Orientation="Horizontal" Margin="0,6,0,12">
            <Button x:Name="SmallTextBtn" Content="Small"/>
            <Button x:Name="MediumTextBtn" Content="Medium" Margin="6,0,0,0"/>
            <Button x:Name="LargeTextBtn" Content="Large" Margin="6,0,0,0"/>
        </StackPanel>

        <TextBlock Text="Layout" FontWeight="Bold"/>
        <CheckBox x:Name="CompactRowsCheck" Content="Compact table rows" Margin="0,6,0,12"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveBtn" Content="Save" MinWidth="90"/>
            <Button x:Name="CloseBtn" Content="Close" MinWidth="90" Margin="6,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>
